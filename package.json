{
  "name": "test-react-vercel",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "dev": "nodemon index.js",
    "install-client": "cd front && yarn install",
    "postbuild": "yarn install-client && yarn build",
    "build": "cd front && yarn build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
