{
  "name": "test-react-vercel",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "dev": "nodemon index.js",
    "install-client": "cd client && yarn install",
    "build-client": "cd client && yarn build",
    "vercel-build": "yarn install-client && yarn build-client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
